<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BungkusYuk" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/whiteButtonColorDarkMode</item>
        <!-- Full Background -->
        <item name="colorPrimaryVariant">@color/brown_dark_fake</item>
        <item name="colorOnPrimary">@color/brown_med</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/whiteButtonColorDarkMode</item>
        <!-- Toolbar -->
        <item name="colorSecondaryVariant">@color/brown_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:navigationBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/poppins</item>
    </style>
</resources>